version: '1'

# Define all entity types
entity_types:
  Building:
    show_properties: false
    has_designer: true

# On app start, create building entity
entities:
- entity_type: Building
  properties:
    name: Parametric Building Structure

# Meta data
metadata:
    welcome_text: welcome_text.txt
    background_image: background_image.jpg